import torch 
class test_class(torch.nn.Module):
    def __init__(self,):
        super().__init__()
        self.FC_layers = torch.nn.Linear(3, 5)
        self.register_buffer('stuff', torch.zeros(5))
        print('initialization')
        self.counter = torch.tensor(10)

test_obj = test_class()
for param in test_obj.parameters():
    print(param)
print(test_obj.state_dict().keys())

print(f'stuff = {test_obj.stuff}')
test_obj = test_obj.to(torch.device('cuda:0'))
print(f'stuff = {test_obj.stuff}, counter={torch.get_device(test_obj.counter)}')
