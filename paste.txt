                base_model = copy.deepcopy(self.embed_ensemble[0])
                base_model = base_model.to('meta')
                params, buffers = stack_module_state(self.embed_ensemble)
                def f(params, buffers, x):
                    return functional_call(base_model, (params, buffers), (x,))
                embedded = vmap(f, in_dims=(0, 0, None))(params, buffers, xyz_sampled)
                embedded = embedded.permute(1, 0, 2) # (B, E, D)